name: Workflow lint

# Lints this repository's own workflow YAML (update.yml carries the per-repo
# logic here) with the shared actionlint reusable, so a broken expression or a
# shellcheck-visible bug in a run: block fails the PR instead of the next
# scheduled update.

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  workflow-lint:
    uses: Glyndor/.github/.github/workflows/workflow-lint.yml@8ae47ccd49ffdbf59d1acf7e6f9c18165bfba959 # v1.19.0
